import {obj} from '../objliterals';

describe('Object Literals', ()=> {
   it('test', ()=>{
       console.info(obj);
       console.info(obj.toString());
       //console.info(obj.prop42);
   }) 
});